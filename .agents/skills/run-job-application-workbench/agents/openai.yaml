interface:
  display_name: "Job Application Workbench"
  short_description: "Run verified applications with synced trackers"
  default_prompt: "Use $run-job-application-workbench to autonomously run or resume a verified job-application batch, update per-application reports, and sync tracker state."
