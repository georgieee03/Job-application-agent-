interface:
  display_name: "Job Application Workbench"
  short_description: "Run verified applications with the UI tracker"
  default_prompt: "Use $run-job-application-workbench to autonomously run or resume a verified job-application run and keep data/application-tracker.json current."
