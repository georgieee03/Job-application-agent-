interface:
  display_name: "Verify Application State"
  short_description: "Persist, audit, and sync application state"
  default_prompt: "Use $verify-job-application-state to create, update, audit, and sync the tracker, reports, and durable application-workbench handoff."
