interface:
  display_name: "Verify Application State"
  short_description: "Persist and audit tracker state"
  default_prompt: "Use $verify-job-application-state to create, update, reconcile, and audit data/application-tracker.json."
