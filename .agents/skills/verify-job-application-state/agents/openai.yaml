interface:
  display_name: "Verify Application State"
  short_description: "Persist and audit application workflow evidence"
  default_prompt: "Use $verify-job-application-state to create, update, or audit the durable application-workbench handoff."
